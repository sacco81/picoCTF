wget <password's link>  
wget <flag's link>  
wget <ende.py's link>    
$man python (see the commands of python)  
$python ende.py -d flag.txt.en  
enter password  

(-d means decode; -e means encode  
